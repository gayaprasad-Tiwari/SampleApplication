import { CheckDuplicateDirective } from './check-duplicate.directive';

describe('CheckDuplicateDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckDuplicateDirective();
    expect(directive).toBeTruthy();
  });
  
});
